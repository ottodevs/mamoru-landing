{
  "name": "mamoru-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "bun run build && bunx wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "wrangler": "4.139.0"
  }
}
